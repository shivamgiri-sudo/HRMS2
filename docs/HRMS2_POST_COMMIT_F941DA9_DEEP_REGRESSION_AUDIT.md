# HRMS2 — Post-Commit Deep Regression Audit
**Commit**: f941da9 (Phase 0-10 workflow engine)
**Date**: 2026-06-25
**Auditor**: Claude Code static analysis

---

## Audit Table

| # | Item | Expected | Implementation | Status | File | Issue | Fix | Priority |
|---|------|----------|----------------|--------|------|-------|-----|----------|
| 1 | requireRole — super_admin bypass | Any user with role `super_admin` in `user_roles` bypasses all role checks; array of roles supported; 403 on denial | DB query on `user_roles.role_key`, `super_admin` short-circuits before expansion, 403 JSON returned | **Working** | `backend/src/middleware/requireRole.ts:50` | None | — | — |
| 2 | authMiddleware — role on req.authUser | `req.authUser.role` populated from JWT so dashboard/scope helpers can read it | JWT payload only stores `sub` (user id) and `email`; `role` field is **never set** on `req.authUser` at auth time; all route handlers fall back to `(user as any).role ?? "employee"` | **Broken** | `backend/src/middleware/authMiddleware.ts:73`, `backend/src/modules/auth/auth.service.ts:191` | JWT does not include role claim; `req.authUser.role` is always `undefined`; every dashboard/inbox/drilldown endpoint silently defaults to `"employee"` scope for all real users | Add `role` to JWT payload at sign time **or** perform a DB lookup in `requireAuth` and set `req.authUser.role` from `user_roles` | P0 |
| 3 | dashboardScope — SELF_ONLY uses auth_user_id; CUSTOM_SCOPE empty → 1=0 | `SELF_ONLY` must filter by `auth_user_id` not employee id; empty `CUSTOM_SCOPE` must return `1=0` | `buildScopeWhere` and `buildScopeWhereEmployees` correctly use `auth_user_id` for `SELF_ONLY`; empty `BRANCH_ALL`/`PROCESS_ALL` return `1=0` ✓; but **`scopeToSqlWhere`** returns `1=1` (all rows) when `BRANCH_ALL` or `PROCESS_ALL` has empty id arrays, and also returns `1=1` for unknown scope levels | **Risky** | `backend/src/shared/dashboardScope.ts:280,288,320` | `scopeToSqlWhere` lines 280 and 288: `if (scope.branchIds.length === 0) return { sql: "1=1" }` — should be `1=0`; line 320 default falls through to `1=1` | Change empty-array guards in `scopeToSqlWhere` to return `1=0`; change default to `1=0` | P1 |
| 4 | Dashboard APIs — scope applied in SQL | All metric endpoints must inject scope WHERE into data queries, not just resolve scope for the response | `resolveDashboardScope` is called for `/summary`, `/metric-values`, `/filters`, `/drilldown`, `/root-causes`; scope is passed to metric service functions that use it | **Partial** | `backend/src/modules/dashboards/dashboard.routes.ts` | `/root-causes` queries `task_tat_instance`, `candidate_name_match_summary`, and `ats_onboarding_bridge` without applying scope filter — returns global data to all roles; `/owner-accountability` has no scope filter at all | Apply `buildScopeWhere` WHERE clause to root-cause and accountability queries or add explicit role guard | P1 |
| 5 | Work inbox /dashboard — branchId forgery prevention | User cannot inject an arbitrary `branchId` to see another branch's work items | `BRANCH_ALL`: requested `branchId` is checked against `scope.branchIds` whitelist before use ✓; `ORG_ALL`: any `branchId` accepted (intended) | **Working** | `backend/src/modules/work-inbox/work-inbox.routes.ts:63-76` | None for branch forgery. Note: `PROCESS_ALL` scope does not pin `effectiveProcessId` to a default when no filter is supplied — returns unfiltered results | Set `effectiveProcessId = scope.processIds[0]` as default when `PROCESS_ALL` and no query param | P2 |
| 6 | TAT service — createTatInstance, checkAndEscalate, work_item insert | `createTatInstance` inserts `task_tat_instance` row and a `work_item`; escalation inserts `task_escalation_log` and `work_item` for notify role | Both functions implemented. `createTatInstance` inserts to `work_item` with column `assigned_to`; escalation inserts with column `assigned_role` | **Broken** | `backend/src/modules/governance/tat.service.ts:43,109` | Schema (`290_dashboard_analytics_engine.sql`) defines columns `assigned_to_user_id` and `assigned_to_role`; code uses `assigned_to` and `assigned_role` — both INSERTs will error at runtime (unknown column) | Rename `assigned_to` → `assigned_to_user_id` on line 43; rename `assigned_role` → `assigned_to_role` on line 109 | P0 |
| 7 | Name consistency — 8 sources, override-approve/reject, work_item | 8 document sources compared; override-approve and override-reject routes exist; mismatch creates work_item | 6 sources checked (form, aadhaar, pan, bank, education, employee_master, appointment_letter = 7 max with optionals); override-approve and override-reject both exist ✓; work_item insert uses correct columns (`assigned_to_role`) ✓ | **Working** | `backend/src/modules/ats/name-consistency.routes.ts` | Source count is 5–7 depending on optional presence, not a fixed 8; no 7th/8th source (e.g. UAN/DigiLocker) implemented | Document actual source list; add remaining sources if required | P3 |
| 8 | Incentive — exactly 3 steps, finance_approved state, no payroll_hr step | 3-step chain: branch_head → operations_head → finance_head; final state `finance_approved`; `payroll_hr` must NOT be a step approver | `APPROVAL_STEPS` = `{1: branch_head, 2: operations_head, 3: finance_head}` ✓; `finance_approved` state set after step 3 ✓; `payroll_hr` is NOT in approval chain ✓; however `payroll_hr` IS in `requireRole` on batch-create/submit/import routes | **Working** | `backend/src/modules/incentives/incentives.routes.ts:101,97` | `payroll_hr` has upload/submit access (intended per comment), not approval authority — correctly excluded from chain | — | — |
| 9 | E-sign service — all 12 routes, state machine | Routes: create, generate, candidate-esign/initiate, candidate-esign/complete, company-sign/initiate, company-sign/complete, finalize, manual-override/request, manual-override/approve, manual-override/reject, GET request, GET audit = 12 | 12 routes confirmed ✓; each step calls `_getState` before transition; audit log written on every step ✓ | **Working** | `backend/src/modules/letters/appointment-esign.routes.ts`, `appointment-esign.service.ts` | No state transition guard enforcement (any state can call any endpoint); no check that candidate-esign is complete before company-sign | Add `allowedFromStates` guard in each service method using `_getState` | P2 |
| 10 | DPDP — submitRequest inserts work_item for compliance | `submitRequest` inserts into `work_item` (singular) for HR/DPO pickup | `submitRequest` inserts into **`work_items`** (plural); the canonical table created in migration `290` is `work_item` (singular); failure is silently swallowed via `.catch(() => {})` | **Broken** | `backend/src/modules/privacy/dpdp-withdrawal.service.ts:66` | Table name mismatch: `work_items` vs `work_item`; compliance work item silently never created; same bug at line 204 for approval notification | Change `work_items` → `work_item` on lines 66 and 204; verify column names (`reference_id` and `assigned_role` are not in schema — use `entity_id` and `assigned_to_role`) | P1 |
| 11 | Resignation — GET /my, accept/withdraw/close, work item triggers | `GET /my` returns employee's own requests; accept/withdraw/close transitions exist; work items triggered on discussion events | `GET /my` exists at line 320 ✓; `POST /:exitId/accept` and `/:exitId/withdraw` exist ✓; close route exists ✓; work items inserted on discussion and HR-discussion with correct columns (`assigned_to_role`) ✓; work_item INSERTs include non-schema columns `reference_id` and `reference_type` | **Partial** | `backend/src/modules/exit/resignation.routes.ts:105,138` | `reference_id` and `reference_type` are not columns in the `work_item` schema from migration 290 — those INSERTs will fail silently (caught by try/catch) | Replace `reference_id`/`reference_type` with `entity_id`/`entity_type` | P1 |
| 12 | Onboarding — family/nominees/blockers/OTP, section-status | Routes for family-members, nominees, blockers, OTP send/verify, and section-status PUT | `/family-members` ✓, `/nominees` ✓, `/blockers` ✓, `/otp/send` ✓, `/otp/verify` ✓, `/section-status` PUT ✓ | **Working** | `backend/src/modules/ats/onboarding-full.routes.ts` | None | — | — |
| 13 | auth.routes.ts — forgotPasswordOtp | Route at line 265 calls `authService.forgotPasswordOtp(phone)` | `authService` has `forgotPassword(email)` method but **no `forgotPasswordOtp`** method; the route will throw `TypeError: authService.forgotPasswordOtp is not a function` at runtime | **Broken** | `backend/src/modules/auth/auth.routes.ts:270`, `backend/src/modules/auth/auth.service.ts` | `forgotPasswordOtp` method missing from `authService`; route silently catches and returns success-like response, masking the error | Implement `authService.forgotPasswordOtp(phone)` method or rename route to call `forgotPassword` with phone lookup | P0 |
| 14 | payroll.controller.ts — 3 missing service methods | `getEmployeeSalaryHistory`, `listPayrollRecords`, `getPayrollOverview` called on `payrollService` | Inspecting `payroll.service.ts`: `getEmployeeSalaryHistory` — **absent**; `listPayrollRecords` — **absent**; `getPayrollOverview` — **absent**; only `getEmployeeSalary`, `listRuns`, and `listLines` exist | **Broken** | `backend/src/modules/payroll/payroll.controller.ts:59,86,96`, `backend/src/modules/payroll/payroll.service.ts` | Three controller methods call undefined service functions → 500 at runtime on those routes (`/salary-assignments/:id/history`, `GET /records`, `GET /overview`) | Implement the three methods in `payrollService` | P0 |
| 15 | ats.controller.ts — listOnboardingBridges | Controller calls `atsService.listOnboardingBridges(scopeFilter)` | `atsService` (ats.service.ts) has `createOnboardingBridge` and `updateOnboardingBridge` but **no `listOnboardingBridges`** method | **Broken** | `backend/src/modules/ats/ats.controller.ts:83`, `backend/src/modules/ats/ats.service.ts` | Calling undefined method → 500 on `GET /ats/onboarding-bridges` | Implement `atsService.listOnboardingBridges(scopeFilter)` | P0 |
| 16 | leave.controller.ts — arg count | `leaveService.submitRequest` called with correct arguments | `submitRequest(input)` — one argument, matching the service signature `submitRequest(input: LeaveRequestInput)` | **Working** | `backend/src/modules/leave/leave.controller.ts:26` | None | — | — |
| 17 | Dashboard root-causes — scoped or global leak | `/root-causes` queries must respect user scope | TAT, name-mismatch, and onboarding stuck queries execute with no WHERE scope filter — all three return global rows to any authenticated user regardless of branch/process scope | **Broken** | `backend/src/modules/dashboards/dashboard.routes.ts:213-265` | A branch-level user can see TAT breaches and name-mismatch data from other branches | Join on branch/process and apply `buildScopeWhere` for non-ORG_ALL scopes | P1 |
| 18 | Owner accountability — scoped | `/owner-accountability` must respect scope | Endpoint queries `work_item` globally with no scope filter; any authenticated user receives org-wide accountability stats | **Broken** | `backend/src/modules/dashboards/dashboard.routes.ts:276` | Global work_item aggregation exposed to all roles | Apply scope WHERE on `branch_id` or `assigned_to_role` where applicable; or restrict to ORG_ALL roles only | P1 |
| 19 | scopeToSqlWhere — CUSTOM_SCOPE/unknown → 1=0 | Empty `CUSTOM_SCOPE` and unknown scope levels must return `1=0` (deny-by-default) | `CUSTOM_SCOPE` empty → returns `1=1` with warn log (line 314); unknown default case → returns `1=1` (line 320); in contrast `buildScopeWhere` and `buildScopeWhereEmployees` correctly return `1=0` for unknown | **Broken** | `backend/src/shared/dashboardScope.ts:314,320` | Data leak: any unrecognised scope level or empty custom scope exposes all rows | Change both `return { sql: "1=1", params: [] }` at lines 314 and 320 to `return { sql: "1=0", params: [] }` | P0 |
| 20 | Work inbox reassign/complete — permission check | Only assigned user or admin/hr should be able to complete/reassign a work item | `completeWorkItem` and `reassignWorkItem` execute unconditionally on any `id` passed — no ownership or role check before the UPDATE | **Risky** | `backend/src/modules/work-inbox/work-inbox.service.ts:62,86` and `work-inbox.routes.ts:83,93` | Any authenticated user can complete or reassign any work item by knowing its UUID | Add pre-update ownership check: `WHERE id = ? AND (assigned_to_user_id = ? OR assigned_to_role IN (...))` or add `requireRole` on the route | P1 |
| 21 | Frontend routes — DPDP/resignation/dashboards in App.tsx | Named pages registered in `App.tsx` for DPDP withdrawal, resignation, and dashboard routes | DPDP: `/privacy/dpdp-withdrawal` and `/compliance/dpdp-withdrawal-admin` registered with lazy-loaded components ✓; Resignation: `/exit/resignation` and `/exit/resignation-command-center` registered but both render `NativePlaceholderPage` (no dedicated resignation UI component yet); Dashboard routes not found by keyword search in excerpt | **Partial** | `src/App.tsx:451-468` | Resignation routes are wired but render placeholder — no functional UI; backend resignation API is complete and not surfaced | Build `NativeResignationPage` and `NativeResignationCommandCenter` components and wire them | P2 |

---

## Summary by Priority

| Priority | Count | Items |
|----------|-------|-------|
| P0 — Runtime crash / data leak blocker | 5 | 2 (role missing on authUser), 6 (TAT wrong column names), 13 (forgotPasswordOtp missing), 14 (3 payroll service methods missing), 15 (listOnboardingBridges missing), 19 (scopeToSqlWhere leak) |
| P1 — Scope/security regression | 5 | 3 (scopeToSqlWhere empty arrays), 4 (root-causes unscoped), 10 (DPDP wrong table), 11 (resignation work_item columns), 17, 18, 20 |
| P2 — Feature incomplete | 2 | 5 (PROCESS_ALL no default), 9 (no state guard in esign), 21 (resignation UI placeholder) |
| P3 — Minor gap | 1 | 7 (source count < 8) |
| Working | 5 | 1, 8, 12, 16, esign service (9 partial) |

---

## Critical Fixes Checklist

```
[ ] authMiddleware: include role in JWT payload or DB-lookup in requireAuth
[ ] scopeToSqlWhere lines 280, 288, 314, 320: change 1=1 → 1=0
[ ] tat.service.ts line 43: assigned_to → assigned_to_user_id
[ ] tat.service.ts line 109: assigned_role → assigned_to_role
[ ] auth.service.ts: implement forgotPasswordOtp(phone) method
[ ] payroll.service.ts: implement getEmployeeSalaryHistory, listPayrollRecords, getPayrollOverview
[ ] ats.service.ts: implement listOnboardingBridges(scopeFilter)
[ ] dpdp-withdrawal.service.ts lines 66, 204: work_items → work_item; fix column names
[ ] resignation.routes.ts lines 105, 138: reference_id/reference_type → entity_id/entity_type
[ ] dashboard.routes.ts root-causes + owner-accountability: apply scope filter
[ ] work-inbox.service.ts: add ownership check in completeWorkItem and reassignWorkItem
```
