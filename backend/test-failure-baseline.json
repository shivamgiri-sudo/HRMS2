{
  "note": "Tests known to be failing. Anything not listed here fails CI. When you fix one, delete its line — the checker also fails if a listed test starts passing, so this file cannot rot.",
  "recorded": 0,
  "failures": [],
  "optionalFailures": [
    "src/modules/employees/__tests__/joiningDocumentTemplatePath.test.ts :: joining-document template path resolution persists only the file name so the row stays portable across machines",
    "src/modules/payroll/__tests__/running-salary.service.test.ts :: computeRunningSalary treats locked night-shift half day in ADR as payroll-visible half day instead of splitting the post-midnight date",
    "src/modules/payroll/__tests__/running-salary.service.test.ts :: computeRunningSalary uses attendance_daily_record payable outcomes for the running month, including half day and approved leave",
    "tests/apr-attendance.service.test.ts :: composeIstDateTime rolls the date forward for TIME >= 24h",
    "tests/apr-attendance.service.test.ts :: composeIstDateTime tags the time as IST so the client can parse it directly",
    "tests/liveTracker.test.ts :: getLiveTracker defaults date to today when not provided"
  ]
}
