export function candidateAssessmentPage(): string {
  return String.raw`<!doctype html>
<html lang="en">
<head>
  <meta charset="utf-8" />
  <meta name="viewport" content="width=device-width,initial-scale=1" />
  <title>Candidate Assessment | MAS Callnet</title>
  <style>
    :root{font-family:Inter,system-ui,-apple-system,BlinkMacSystemFont,"Segoe UI",sans-serif;color:#142236;background:#edf3f7;--primary:#0c756d;--deep:#123e5e;--line:#d8e3ec;--danger:#b42318;--warning:#9a5b00}*{box-sizing:border-box}body{margin:0}.top{background:linear-gradient(135deg,#103956,#0c7c72);color:#fff;padding:17px 22px;position:sticky;top:0;z-index:30;box-shadow:0 6px 24px #071a2a33}.top-row{max-width:1080px;margin:auto;display:flex;align-items:center;justify-content:space-between;gap:15px}.top h1{margin:0;font-size:21px}.top p{margin:4px 0 0;font-size:13px;opacity:.84}.top-timer{text-align:right}.top-timer b{display:block;font-size:22px}.wrap{max-width:1080px;margin:auto;padding:20px}.card{background:#fff;border:1px solid var(--line);border-radius:18px;padding:21px;margin-bottom:15px;box-shadow:0 8px 28px #17324d0e}.hidden{display:none!important}.grid{display:grid;grid-template-columns:repeat(2,minmax(0,1fr));gap:13px}.metrics{display:grid;grid-template-columns:repeat(4,minmax(0,1fr));gap:10px}.metric{background:#f3f7f9;border:1px solid #e2ebf1;border-radius:12px;padding:12px;text-align:center}.metric b{display:block;font-size:24px;color:var(--primary)}.metric span{font-size:11px;text-transform:uppercase;color:#66788a}.field label{display:block;font-size:12px;font-weight:800;margin:0 0 6px;color:#405467}.field input,.field textarea{width:100%;border:1px solid #c9d7e2;border-radius:10px;padding:12px;font:inherit;background:#fff}.field textarea{min-height:125px;resize:vertical}.field input:focus,.field textarea:focus{outline:2px solid #b5e2dd;border-color:var(--primary)}.btn{border:0;border-radius:10px;padding:12px 18px;background:var(--primary);color:#fff;font-weight:850;cursor:pointer}.btn.secondary{background:#41586e}.btn.warning{background:#a56500}.btn.danger{background:var(--danger)}.btn:disabled{opacity:.5;cursor:not-allowed}.row{display:flex;gap:9px;align-items:center;flex-wrap:wrap}.space{justify-content:space-between}.muted{font-size:12px;color:#66788a}.notice{padding:12px 14px;border-radius:10px;background:#eaf7f5;color:#075e56;margin:10px 0}.notice.error{background:#fff0f0;color:#9e1c13}.notice.warning{background:#fff5dc;color:#7d5000}.badge{display:inline-flex;border-radius:999px;background:#e8f4f2;color:#075e56;padding:5px 9px;font-size:11px;font-weight:850}.badge.blue{background:#e9f1ff;color:#245095}.progress{height:9px;border-radius:99px;background:#dce7ed;overflow:hidden}.progress>div{height:100%;width:0;background:linear-gradient(90deg,#0c756d,#2aa89d);transition:width .25s}.question{padding:17px 0;border-bottom:1px solid #e8eef3}.question:last-child{border-bottom:0}.question-title{font-weight:800;line-height:1.45;margin:6px 0 10px}.option{display:block;border:1px solid #d8e3ec;border-radius:10px;padding:10px 11px;margin:7px 0;cursor:pointer}.option:hover{background:#f5faf9}.option input{margin-right:8px}.save-state{min-height:17px;margin-top:5px}.typing-passage{background:#f5f7fa;border:1px solid #d8e2ea;border-radius:12px;padding:17px;line-height:1.8;white-space:pre-wrap;user-select:none}.typing-area{min-height:200px!important;font-family:ui-monospace,SFMono-Regular,Consolas,monospace;line-height:1.6}.typing-note{font-weight:750;color:#405467;margin:10px 0}.diff{display:flex;gap:5px;flex-wrap:wrap;line-height:1.8}.word{padding:2px 5px;border-radius:5px}.correct{background:#e3f7eb}.incorrect{background:#ffe2e2}.missing{background:#fff0bd;text-decoration:line-through}.extra{background:#e9e4ff}.legend{display:flex;gap:10px;flex-wrap:wrap;font-size:12px;margin:9px 0}.legend span{padding:3px 7px;border-radius:5px}.sticky-actions{position:sticky;bottom:0;z-index:10;box-shadow:0 -8px 25px #071a2a15}.result-score{font-size:48px;font-weight:900;color:var(--primary)}.section-score{display:grid;grid-template-columns:1fr auto;gap:10px;border-bottom:1px solid #e5edf3;padding:10px 0}.instructions li{margin:8px 0;line-height:1.45}.candidate-line{font-weight:700}.spinner{display:inline-block;width:16px;height:16px;border:2px solid #ffffff66;border-top-color:#fff;border-radius:50%;animation:spin .7s linear infinite}@keyframes spin{to{transform:rotate(360deg)}}@media(max-width:700px){.wrap{padding:11px}.card{padding:15px}.grid,.metrics{grid-template-columns:1fr 1fr}.top{padding:13px}.top h1{font-size:17px}.top p{font-size:11px}.top-timer b{font-size:18px}.result-score{font-size:38px}}@media(max-width:430px){.grid,.metrics{grid-template-columns:1fr}.top-row{align-items:flex-start}}
  </style>
</head>
<body>
<header class="top"><div class="top-row"><div><h1>MAS Callnet Candidate Assessment</h1><p>Complete the assessment while waiting for your HR interview</p></div><div id="headerTimer" class="top-timer hidden"><b id="timeRemaining">--:--</b><span>Time remaining</span></div></div></header>
<main class="wrap">
  <section id="lookup" class="card">
    <h2>Open your assessment</h2>
    <p class="muted">Enter the queue token received after candidate registration and the same mobile number used in registration.</p>
    <div class="grid">
      <div class="field"><label>Queue Token</label><input id="queueToken" maxlength="100" autocomplete="off" placeholder="Example: N-104" /></div>
      <div class="field"><label>Registered Mobile Number</label><input id="mobile" maxlength="10" inputmode="numeric" autocomplete="off" placeholder="10-digit mobile number" /></div>
    </div>
    <div id="lookupMsg"></div>
    <button id="openBtn" class="btn">Open Assessment</button>
  </section>

  <section id="intro" class="card hidden">
    <div class="row"><span id="templateBadge" class="badge"></span><span class="badge blue">Assessment attempt: 1 only</span><span class="badge blue">Typing attempts: maximum 2</span></div>
    <h2 id="templateName"></h2>
    <div id="candidateSummary" class="candidate-line"></div>
    <div id="introMeta" class="notice"></div>
    <ul id="instructions" class="instructions"></ul>
    <div class="notice warning"><b>Important:</b> Once the assessment is started, its timer continues even if the browser is refreshed or closed.</div>
    <button id="startBtn" class="btn">Start Assessment</button>
  </section>

  <section id="assessment" class="hidden">
    <div class="card">
      <div class="row space"><div><span id="statusBadge" class="badge"></span><h2 id="activeName" style="margin:8px 0 0"></h2></div><div class="muted">Answers save automatically</div></div>
      <div class="progress"><div id="progressBar"></div></div><p id="progressText" class="muted"></p>
    </div>
    <div id="questions" class="card"></div>
    <div id="typingCard" class="card hidden"></div>
    <div class="card sticky-actions"><div id="submitMsg"></div><button id="submitAssessment" class="btn">Submit Complete Assessment</button></div>
  </section>

  <section id="result" class="card hidden"></section>
</main>
<script>
(()=>{"use strict";
const API="/api/ats-ext";const $=id=>document.getElementById(id);let token="";let session=null;let answers=new Map();let countdownTimer=null;let typingTimer=null;let typingState=null;let finalSubmitting=false;let visibilityEvents=0;
const esc=value=>String(value??"").replace(/[&<>"']/g,ch=>({"&":"&amp;","<":"&lt;",">":"&gt;",'"':"&quot;","'":"&#39;"}[ch]));
async function api(path,options={}){const response=await fetch(API+path,{...options,headers:{"Content-Type":"application/json",...(options.headers||{})}});const payload=await response.json().catch(()=>({}));if(!response.ok){const error=new Error(payload.message||"Request failed");error.code=payload.code;throw error}return payload.data}
function show(id){$(id).classList.remove("hidden")}function hide(id){$(id).classList.add("hidden")}
function message(id,text,type="notice"){const el=$(id);if(el)el.innerHTML=text?'<div class="'+type+'">'+esc(text)+"</div>":""}
function formatTime(seconds){const safe=Math.max(0,Math.floor(Number(seconds)||0));const minutes=Math.floor(safe/60);return String(minutes).padStart(2,"0")+":"+String(safe%60).padStart(2,"0")}
function storeToken(value){token=value;localStorage.setItem("ats_candidate_assessment_token",value);history.replaceState(null,"",location.pathname+"#token="+encodeURIComponent(value))}
function clearToken(){localStorage.removeItem("ats_candidate_assessment_token")}
function currentTemplate(){return session?.assessment?.template||session?.template||null}
function completedStatus(status){return status==="completed"||status==="manual_review"}
function terminalStatus(status){return completedStatus(status)||["expired","cancelled","technical_error","skipped"].includes(status)}

async function openAssessment(){try{$("openBtn").disabled=true;$("openBtn").innerHTML='<span class="spinner"></span> Checking';message("lookupMsg","Checking candidate registration...");const input={queueToken:$("queueToken").value.trim(),mobile:$("mobile").value.replace(/\D/g,"")};const assigned=await api("/assessment/lookup",{method:"POST",body:JSON.stringify(input)});storeToken(assigned.token);session=await api("/assessment/session/"+encodeURIComponent(token));renderEntry()}catch(error){message("lookupMsg",error.message,"notice error")}finally{$("openBtn").disabled=false;$("openBtn").textContent="Open Assessment"}}

function renderEntry(){if(!session)return;hide("lookup");hide("intro");hide("assessment");hide("result");const status=session.assessment.status;if(completedStatus(status)){renderResult();return}if(terminalStatus(status)){show("result");$("result").innerHTML='<h2>Assessment unavailable</h2><div class="notice error">This assessment is '+esc(status.replaceAll("_"," "))+'. Please speak with the recruiter.</div>';clearCountdown();return}if(status==="assigned"){renderIntro();return}show("assessment");renderAssessment()}

function renderIntro(){show("intro");const template=currentTemplate();$("templateBadge").textContent=template.process.replaceAll("_"," ")+" / "+template.role.replaceAll("_"," ");$("templateName").textContent=template.name;$("candidateSummary").textContent=(session.candidate?.name||"Candidate")+(session.candidate?.code?" · "+session.candidate.code:"")+(session.candidate?.queueToken?" · Token "+session.candidate.queueToken:"");$("introMeta").textContent=template.durationMinutes+" minutes · "+template.questionCount+" questions · Passing benchmark "+template.passingPercentage+"%";$("instructions").innerHTML=(template.instructions||[]).map(item=>"<li>"+esc(item)+"</li>").join("")}

async function startAssessment(){try{$("startBtn").disabled=true;$("startBtn").innerHTML='<span class="spinner"></span> Starting';session=await api("/assessment/session/"+encodeURIComponent(token)+"/start",{method:"POST",body:"{}"});renderEntry()}catch(error){alert(error.message)}finally{$("startBtn").disabled=false;$("startBtn").textContent="Start Assessment"}}

function renderAssessment(){const template=currentTemplate();$("activeName").textContent=template.name;$("statusBadge").textContent=session.assessment.status.replaceAll("_"," ");answers=new Map((session.responses||[]).map(response=>[response.questionId,response.answer]));$("questions").innerHTML="<h2>Assessment Questions</h2>"+template.questions.map((question,index)=>renderQuestion(question,index+1)).join("");wireQuestionEvents();renderTyping();updateProgress();startCountdown()}

function renderQuestion(question,index){const existing=answers.get(question.id);let control="";if(question.type==="single"){control=(question.options||[]).map(option=>'<label class="option"><input type="radio" name="question_'+esc(question.id)+'" value="'+esc(option)+'" '+(existing===option?"checked":"")+'>'+esc(option)+"</label>").join("")}else if(question.type==="multi"){control=(question.options||[]).map(option=>'<label class="option"><input type="checkbox" data-multi="'+esc(question.id)+'" value="'+esc(option)+'" '+(Array.isArray(existing)&&existing.includes(option)?"checked":"")+'>'+esc(option)+"</label>").join("")}else{control='<textarea data-text="'+esc(question.id)+'" maxlength="10000" placeholder="Write a clear and structured answer">'+esc(existing||"")+"</textarea>"}return '<div class="question" id="question_'+esc(question.id)+'"><div class="muted">Question '+index+" · "+esc(question.sectionTitle)+" · "+esc(question.marks)+" marks</div><div class=\"question-title\">"+esc(question.prompt)+"</div>"+control+'<div class="muted save-state" id="save_'+esc(question.id)+'"></div></div>'}

function wireQuestionEvents(){document.querySelectorAll('input[type="radio"][name^="question_"]').forEach(input=>input.addEventListener("change",event=>saveAnswer(event.target.name.slice(9),event.target.value)));document.querySelectorAll("[data-multi]").forEach(input=>input.addEventListener("change",event=>{const questionId=event.target.dataset.multi;const values=[...document.querySelectorAll('[data-multi="'+questionId+'"]:checked')].map(item=>item.value);saveAnswer(questionId,values)}));document.querySelectorAll("[data-text]").forEach(input=>{let timer=null;input.addEventListener("input",event=>{clearTimeout(timer);timer=setTimeout(()=>saveAnswer(event.target.dataset.text,event.target.value),650)});input.addEventListener("blur",event=>saveAnswer(event.target.dataset.text,event.target.value))})}

async function saveAnswer(questionId,value){answers.set(questionId,value);updateProgress();const state=$("save_"+questionId);if(state)state.textContent="Saving...";try{await api("/assessment/session/"+encodeURIComponent(token)+"/responses/"+encodeURIComponent(questionId),{method:"PUT",body:JSON.stringify({answer:value})});if(state)state.textContent="Saved"}catch(error){if(state)state.textContent="Not saved: "+error.message;if(error.code==="ASSESSMENT_TIME_ENDED")await refreshSession()}}

function answered(value){return Array.isArray(value)?value.length>0:typeof value==="string"?value.trim().length>0:value!=null}
function updateProgress(){const template=currentTemplate();if(!template)return;const questionDone=template.questions.filter(question=>answered(answers.get(question.id))).length;const typingRequired=template.typing.required;const typingDone=(session.typingAttempts||[]).some(attempt=>attempt.submittedAt);const total=template.questions.length+(typingRequired?1:0);const complete=questionDone+(typingRequired&&typingDone?1:0);const percentage=total?Math.round(complete/total*100):0;$("progressBar").style.width=percentage+"%";$("progressText").textContent=complete+" of "+total+" required parts completed"}

function startCountdown(){clearCountdown();show("headerTimer");let remaining=session.assessment.remainingSeconds;if(remaining==null&&session.assessment.expiresAt)remaining=Math.max(0,Math.ceil((new Date(session.assessment.expiresAt).getTime()-Date.now())/1000));function tick(){const deadline=session.assessment.expiresAt?new Date(session.assessment.expiresAt).getTime():null;if(deadline!=null)remaining=Math.max(0,Math.ceil((deadline-Date.now())/1000));$("timeRemaining").textContent=formatTime(remaining);if(remaining<=300)$("timeRemaining").style.color="#ffe080";if(remaining<=0){clearCountdown();finalSubmit(true)}}tick();countdownTimer=setInterval(tick,1000)}
function clearCountdown(){if(countdownTimer)clearInterval(countdownTimer);countdownTimer=null;hide("headerTimer")}

function renderTyping(){const template=currentTemplate();if(!template.typing.required){hide("typingCard");return}show("typingCard");const attempts=session.typingAttempts||[];const active=attempts.find(attempt=>attempt.active&&!attempt.submittedAt);if(active){typingState={...active,id:active.id,passage:active.passage,durationSeconds:active.durationSeconds,startedAt:active.startedAt,attemptNo:active.attemptNo};renderTypingActive();return}const submitted=attempts.filter(attempt=>attempt.submittedAt);let html='<h2>Typing Test</h2><p class="typing-note">Live speed and overall estimated accuracy are shown. No character or word is marked correct or incorrect while you type.</p>';submitted.forEach(attempt=>{html+='<div class="notice"><b>Attempt '+esc(attempt.attemptNo)+":</b> "+Number(attempt.netWpm||0).toFixed(1)+" net WPM · "+Number(attempt.accuracy||0).toFixed(1)+"% accuracy · "+Number(attempt.score||0).toFixed(1)+"% score</div>"});const used=Number(session.assessment.typingAttemptsUsed||0);if(used<2){html+='<button id="typingStart" class="btn '+(used?"warning":"")+'">'+(used?"Start Final Typing Attempt":"Start Typing Attempt 1")+"</button>"}else{html+='<div class="notice warning">Both typing attempts have been used. The better submitted result will be considered.</div>'}$("typingCard").innerHTML=html;if($("typingStart"))$("typingStart").onclick=startTyping}

async function startTyping(){try{$("typingStart").disabled=true;typingState=await api("/assessment/session/"+encodeURIComponent(token)+"/typing/start",{method:"POST",body:"{}"});renderTypingActive()}catch(error){alert(error.message)}}

function levenshtein(left,right){const a=Array.from(left),b=Array.from(right);let previous=Array.from({length:b.length+1},(_,index)=>index);for(let row=1;row<=a.length;row++){const current=[row];for(let column=1;column<=b.length;column++){current[column]=Math.min(current[column-1]+1,previous[column]+1,previous[column-1]+(a[row-1]===b[column-1]?0:1))}previous=current}return previous[b.length]}
function liveMetrics(){if(!typingState||!$("typingInput"))return;const text=$("typingInput").value;const started=new Date(typingState.startedAt).getTime();const elapsed=Math.max(1,Math.floor((Date.now()-started)/1000));const gross=(Array.from(text).length/5)/(elapsed/60);const distance=levenshtein(typingState.passage,text);const denominator=Math.max(Array.from(typingState.passage).length,Array.from(text).length,1);const accuracy=Math.max(0,(denominator-distance)/denominator*100);$("typingElapsed").textContent=formatTime(elapsed);$("typingWpm").textContent=gross.toFixed(1);$("typingAccuracy").textContent=accuracy.toFixed(1)+"%";$("typingCharacters").textContent=Array.from(text).length;const remaining=Number(typingState.durationSeconds)-elapsed;$("typingRemaining").textContent=formatTime(remaining);if(remaining<=0)submitTyping(true)}

function renderTypingActive(){if(typingTimer)clearInterval(typingTimer);let backspaces=0;let pasteAttempts=0;$("typingCard").innerHTML='<div class="row space"><h2>Typing Attempt '+esc(typingState.attemptNo)+' of 2</h2><span class="badge blue">Remaining: <b id="typingRemaining">--:--</b></span></div><div class="metrics"><div class="metric"><b id="typingElapsed">00:00</b><span>Elapsed</span></div><div class="metric"><b id="typingWpm">0.0</b><span>Gross WPM</span></div><div class="metric"><b id="typingAccuracy">100%</b><span>Est. Accuracy</span></div><div class="metric"><b id="typingCharacters">0</b><span>Characters</span></div></div><div class="notice warning"><b>No live correction guidance:</b> individual correct, incorrect, missing, or extra characters are not shown until this attempt is submitted.</div><div id="typingPassage" class="typing-passage">'+esc(typingState.passage)+'</div><div class="field" style="margin-top:12px"><label>Type the passage exactly as shown</label><textarea id="typingInput" class="typing-area" autocomplete="off" autocorrect="off" autocapitalize="off" spellcheck="false"></textarea></div><div id="typingMsg"></div><button id="typingSubmit" class="btn">Submit Typing Attempt</button>';$("typingPassage").addEventListener("copy",event=>{event.preventDefault();recordIntegrity("copy_attempt",{area:"typing_passage"})});$("typingInput").addEventListener("keydown",event=>{if(event.key==="Backspace")backspaces+=1;typingState.backspaceCount=backspaces});$("typingInput").addEventListener("paste",event=>{event.preventDefault();pasteAttempts+=1;typingState.pasteAttempts=pasteAttempts;recordIntegrity("paste_attempt",{area:"typing_input"});message("typingMsg","Pasting is disabled for the typing test.","notice warning")});const draftKey="ats_typing_draft_"+typingState.id;$("typingInput").value=localStorage.getItem(draftKey)||"";$("typingInput").addEventListener("input",event=>{localStorage.setItem(draftKey,event.target.value);liveMetrics()});$("typingSubmit").onclick=()=>submitTyping(false);$("typingInput").focus();liveMetrics();typingTimer=setInterval(liveMetrics,500)}

async function submitTyping(automatic){if(!typingState||typingState.submitting)return;typingState.submitting=true;if(typingTimer)clearInterval(typingTimer);typingTimer=null;if($("typingSubmit"))$("typingSubmit").disabled=true;try{const result=await api("/assessment/session/"+encodeURIComponent(token)+"/typing/"+encodeURIComponent(typingState.id)+"/submit",{method:"POST",body:JSON.stringify({typedText:$("typingInput")?.value||"",backspaceCount:typingState.backspaceCount||0,pasteAttempts:typingState.pasteAttempts||0})});localStorage.removeItem("ats_typing_draft_"+typingState.id);renderTypingResult(result,automatic);session=await api("/assessment/session/"+encodeURIComponent(token));updateProgress()}catch(error){message("typingMsg",error.message,"notice error");typingState.submitting=false;if($("typingSubmit"))$("typingSubmit").disabled=false}}

function renderTypingResult(result,automatic){const detailed=(result.diff||[]).map(item=>'<span class="word '+esc(item.status)+'" title="Expected: '+esc(item.expected||"—")+'">'+esc(item.typed||item.expected||"—")+"</span>").join("");$("typingCard").innerHTML='<h2>Typing Attempt '+esc(result.attemptNo)+' Result</h2>'+(automatic?'<div class="notice warning">Time ended and the typing attempt was submitted automatically.</div>':"")+'<div class="metrics"><div class="metric"><b>'+Number(result.netWpm||0).toFixed(1)+'</b><span>Net WPM</span></div><div class="metric"><b>'+Number(result.accuracy||0).toFixed(1)+'%</b><span>Accuracy</span></div><div class="metric"><b>'+esc(result.correctWords||0)+'</b><span>Correct Words</span></div><div class="metric"><b>'+esc(result.incorrectWords||0)+'</b><span>Word Errors</span></div></div><div class="notice '+(result.passedBenchmark?"":"warning")+'">Benchmark: '+esc(result.benchmark.minNetWpm)+' net WPM and '+esc(result.benchmark.minAccuracy)+'% accuracy — <b>'+(result.passedBenchmark?"Met":"Not met")+'</b></div><h3>Detailed feedback</h3><div class="legend"><span class="correct">Correct</span><span class="incorrect">Incorrect</span><span class="missing">Missing</span><span class="extra">Extra</span></div><div class="diff">'+detailed+'</div>'+(result.attemptsRemaining>0?'<button id="typingAgain" class="btn warning" style="margin-top:14px">Use Final Typing Attempt</button>':'<div class="notice warning">No typing attempts remain. The better result will be used.</div>');if($("typingAgain"))$("typingAgain").onclick=async()=>{session=await api("/assessment/session/"+encodeURIComponent(token));renderTyping()};typingState=null}

async function finalSubmit(automatic=false){if(finalSubmitting)return;if(!automatic&&!confirm("Submit the complete assessment? Only one assessment attempt is allowed and submission cannot be reversed."))return;finalSubmitting=true;if($("submitAssessment")){$("submitAssessment").disabled=true;$("submitAssessment").innerHTML='<span class="spinner"></span> Submitting'}message("submitMsg",automatic?"Assessment time ended. Submitting saved responses...":"Calculating and saving your result...");try{await api("/assessment/session/"+encodeURIComponent(token)+"/submit",{method:"POST",body:"{}"});session=await api("/assessment/session/"+encodeURIComponent(token));renderResult(automatic)}catch(error){if(error.code==="ASSESSMENT_INCOMPLETE"&&!automatic){message("submitMsg",error.message,"notice error");finalSubmitting=false;$("submitAssessment").disabled=false;$("submitAssessment").textContent="Submit Complete Assessment";return}try{session=await api("/assessment/session/"+encodeURIComponent(token));if(completedStatus(session.assessment.status)){renderResult(automatic);return}}catch{}message("submitMsg",error.message,"notice error");finalSubmitting=false;if($("submitAssessment")){$("submitAssessment").disabled=false;$("submitAssessment").textContent="Submit Complete Assessment"}}}

function renderResult(autoSubmitted=false){clearCountdown();if(typingTimer)clearInterval(typingTimer);hide("assessment");hide("intro");show("result");const assessment=session.assessment;const pending=assessment.status==="manual_review";const sections=Object.entries(assessment.sectionScores||{}).map(([key,value])=>'<div class="section-score"><span>'+esc(value.title||key)+'</span><b>'+Number(value.percentage||0).toFixed(1)+'%</b></div>').join("");$("result").innerHTML='<span class="badge">Assessment submitted</span><h2>Thank you, '+esc(session.candidate?.name||"Candidate")+'</h2>'+(autoSubmitted?'<div class="notice warning">The assessment was submitted automatically when time ended.</div>':"")+'<div class="result-score">'+Number(assessment.percentage||0).toFixed(1)+'%</div><p>'+ (pending?'<span class="badge blue">Manual review pending</span>':'<span class="badge">'+esc(assessment.result||"completed")+'</span>')+'</p>'+sections+'<div class="notice">'+(pending?"Your objective and typing sections are saved. HR or Quality will review the written response before finalising the assessment result.":"Your assessment result is saved in your HRMS candidate profile.")+" Please remain available for the HR interview.</div>";clearToken();finalSubmitting=false}

async function refreshSession(){if(!token)return;try{session=await api("/assessment/session/"+encodeURIComponent(token));renderEntry()}catch(error){show("result");hide("assessment");$("result").innerHTML='<h2>Unable to reopen assessment</h2><div class="notice error">'+esc(error.message)+"</div>"}}
async function recordIntegrity(eventType,details){if(!token||!session||session.assessment.status!=="in_progress")return;try{await api("/assessment/session/"+encodeURIComponent(token)+"/integrity",{method:"POST",body:JSON.stringify({eventType,details})})}catch{}}

$("openBtn").onclick=openAssessment;$("startBtn").onclick=startAssessment;$("submitAssessment").onclick=()=>finalSubmit(false);$("mobile").oninput=event=>event.target.value=event.target.value.replace(/\D/g,"").slice(0,10);$("mobile").onkeydown=event=>{if(event.key==="Enter")openAssessment()};$("queueToken").onkeydown=event=>{if(event.key==="Enter")openAssessment()};document.addEventListener("visibilitychange",()=>{if(document.hidden&&session?.assessment?.status==="in_progress"){visibilityEvents+=1;recordIntegrity("tab_hidden",{count:visibilityEvents})}});window.addEventListener("blur",()=>{if(session?.assessment?.status==="in_progress")recordIntegrity("window_blur",{})});window.addEventListener("beforeunload",event=>{if(session?.assessment?.status==="in_progress"){event.preventDefault();event.returnValue=""}});
const queueFromQuery=new URLSearchParams(location.search).get("queueToken");if(queueFromQuery)$("queueToken").value=queueFromQuery;const savedToken=new URLSearchParams(location.hash.slice(1)).get("token")||localStorage.getItem("ats_candidate_assessment_token");if(savedToken){token=savedToken;storeToken(savedToken);refreshSession()}
})();
</script>
</body>
</html>`;
}
