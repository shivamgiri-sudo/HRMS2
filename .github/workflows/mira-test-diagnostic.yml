name: Mira Test Diagnostic

on:
  pull_request:
    branches: [main]
    paths:
      - "backend/**"
      - ".github/workflows/mira-test-diagnostic.yml"

permissions:
  contents: read

jobs:
  diagnose:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: 22
          cache: npm
          cache-dependency-path: backend/package-lock.json
      - run: npm --prefix backend ci
      - name: Capture full backend test report
        run: |
          set +e
          npm --prefix backend exec -- vitest run --reporter=json > /tmp/backend-test-results.json 2>&1
          exit 0
      - name: Upload diagnostic report
        uses: actions/upload-artifact@v4
        with:
          name: mira-backend-test-results
          path: /tmp/backend-test-results.json
          if-no-files-found: error
          retention-days: 1
